# generated by Buildyard, do not edit.
before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq 
notifications:
  email:
    on_success: never
language: cpp
script:
 - git clone --depth 10 https://github.com/Eyescale/Buildyard.git
 - cd Buildyard
 - git clone --depth 10 https://github.com/VMML/config.git config.vmml
 - make -j8 vmmlib-test ARGS=-V
